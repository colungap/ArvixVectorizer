Command line Chroma/LLama vector database.
dependencies can be installed using <pip install -r requirements.txt>
and the database is initialized/populated by running the driver file arvix_vectorizer ie <python arvix_vectorizer.py>